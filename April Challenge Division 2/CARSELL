
t=int(input())
while(t>0):
    t-=1
    pr=0
    n=int(input())
    p=[int(p) for p in input().split()]
    p.sort(reverse=True)
    i=1
    for j in p:
        #p.sort(reverse=True)
        i-=1
        if(i+j>0):
            pr+=i+j 
    print(pr%1000000007)
